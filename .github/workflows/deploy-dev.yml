name: Deploy to Dev Environment

on:
  push:
    branches:
      - main
      - develop
  workflow_dispatch:

env:
  REGISTRY: ${{ secrets.HARBOR_REGISTRY }}
  IMAGE_PREFIX: project
  NAMESPACE: dev

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: Checkout service code
        uses: actions/checkout@v4
        with:
          path: service

      - name: Checkout config repo
        uses: actions/checkout@v4
        with:
          repository: Rensights/rensights
          path: config
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'latest'

      - name: Set up Helm
        uses: azure/setup-helm@v3
        with:
          version: 'latest'

      - name: Configure kubectl
        run: |
          echo "${{ secrets.DEV_KUBECONFIG }}" | base64 -d > /tmp/kubeconfig
          export KUBECONFIG=/tmp/kubeconfig
          kubectl cluster-info

      - name: Deploy admin-backend
        run: |
          export KUBECONFIG=/tmp/kubeconfig
          SHA=$(git rev-parse --short HEAD)
          helm upgrade --install admin-backend config/charts/admin-backend \
            --namespace ${NAMESPACE} \
            --create-namespace \
            -f config/env-values/dev/global.yaml \
            -f config/env-values/dev/admin-backend.yaml \
            --set image.tag=sha-${SHA} \
            --set image.repository=${REGISTRY}/${IMAGE_PREFIX}/admin-backend \
            --wait --timeout=5m

      - name: Verify deployment
        run: |
          export KUBECONFIG=/tmp/kubeconfig
          kubectl get pods -n ${NAMESPACE} -l app=admin-backend
          kubectl get svc -n ${NAMESPACE} -l app=admin-backend

